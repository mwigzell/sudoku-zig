// Wasm deploy entry — stub until structured JSON exports land (#31).
// REPL shell (step/page_bytes_out/WasmHost) removed in #34; artifact.wasm
// still compiles and embeds for serve + glue contract smoke test.
pub fn main() void {}
