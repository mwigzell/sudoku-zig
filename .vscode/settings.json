{
  "zig.path": "${env:HOME}/.local/tools/zig-latest/zig",
  "zig.zls.enabled": "on",
  "zig.testArgs": [
    "build",
    "test",
    "-Dtest-filter=${filter}"
  ],
  "zig.debugTestArgs": [
    "build",
    "debug-test",
    "-Dtest-filter=${filter}"
  ]
}
