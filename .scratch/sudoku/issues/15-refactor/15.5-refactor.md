Status: done

## Triage

Date: 2026-07-18

Triage notes: assembleRenderSnapshot() is eliminated — BoardView replaces it entirely through the render chain, zero-copy. Five dead files excised. root.zig imports pruned to 7 modules.

## Blocked By

(none) — all blockers cleared (15.3 closed, 15.4 closed)

## What to build

Rewire the render path through BoardView, drop dead modules, land identical `zig build run` output. TDD order: test before code for each change.

---

### Change 1 — MockRenderer captures [9][9]CellValue from BoardView

**Why:** StdoutRenderer and RenderSnapshot are gone. MockRenderer needs a new contract that accepts BoardView and records a value copy.

**Test (write first):** `"MockRenderer: copies BoardView flat cells into [9][9]CellValue"`
- Build a board with known values at specific positions
- Call `mock.render(view)` 
- Assert `last_rendered_cells` captures all 81 values including givens, empties, and player-set cells
- Assert `call_count` incremented

**Code (write after test):** Rewrite `mock_renderer.zig`:
- Drop import of `renderer.zig`, add imports for `board.zig` + `cell.zig`
- Field `last_snapshot: ?RenderSnapshot` → `last_rendered_cells: ?[9][9]cell.CellValue`
- Method signature: `render(view: board.BoardView)` instead of `render(snap: RenderSnapshot)`
- Copy loop walks `view.board.cells[idx].value` into `[9][9]CellValue`

---

## Break out Changes 2–4 into discrete test/code checkpoints

### Context

After Change 1, GameEngine.render() already calls `asView()` and all 49 tests pass.
The remaining work is dead-code removal (board.zig), binary rewire (main.zig), and file cleanup — each independently verifiable.

---

### Test T1 — board.zig: dead `assembleRenderSnapshot` is safe to remove

**Verify before code:** Run `grep -rn 'assembleRenderSnapshot' src/` — the only hits should be inside `board.zig` itself (the function definition + its 2 co-located tests). No other file calls it.

**Code:** From `board.zig`:
1. Remove `const renderer = @import("renderer.zig");` import line (no other callees)
2. Delete `pub fn assembleRenderSnapshot(self: Board) renderer.RenderSnapshot { ... }` method
3. Delete its 2 test blocks

**Verify after:** `zig test src/board.zig` passes (fewer tests, zero failures).

---

### Test T2 — main.zig: rewire through AsciiRenderer, binary runs identically

**Verify before code:** Capture current output: `cd /home/mark/Dev/src/sudoku && zig build run > /tmp/before-15.5.txt` (if it still compiles) OR note what the current binary prints.

**Code:** Replace main.zig body:
- `std_renderer.StdoutRenderer` → `ascii_renderer.AsciiRenderer`
- `io_sink.IoSink.initStdout(...)` → `std.Io.File.stdout().writer(init.io, &.{} )`
- Pass `&stdout_writer.interface` to AsciiRenderer.init

**Verify after:**
- `zig build run > /tmp/after-15.5.txt`
- `diff /tmp/before-15.5.txt /tmp/after-15.5.txt` → zero diff (identical ASCII board output)
- `zig test src/root.zig` → all tests still pass

---

### Test T3 — Delete dead files + prune root.zig to 7 imports

**Verify before code:** After T1+T2 land and green, list files to go:
| File | Reason |
|------|--------|
| `src/std_renderer.zig` | Replaced by AsciiRenderer; tests duplicated in ascii_renderer (#15.4) |
| `src/renderer.zig` | RenderCell/RenderSnapshot — both eliminated |
| `src/io_sink.zig` | IoSink consumer was StdoutRenderer only |
| `src/grid.zig` | Dead code since Board owns flat `[81]Cell` |
| `src/box.zig` | Unused since #15.1 |

**Code:**
1. `git rm src/std_renderer.zig src/renderer.zig src/io_sink.zig src/grid.zig src/box.zig`
2. Edit `src/root.zig`: remove matching imports + tuple entries, leaving 7 modules only:
   - cell, board, game_engine, mock_renderer, puzzle_gen, logger, ascii_renderer

**Verify after:**
- `zig test src/root.zig` → all remaining tests discovered and pass
- `zig build run` → produces identical output
- `zig build cov` → coverage maintained or improved

---


### Acceptance criteria

- [ ] `zig build run` produces identical ASCII board output (visual diff = zero)
- [ ] GameEngine.render() walks BoardView; assembleRenderSnapshot() removed from board.zig and its 2 co-located tests
- [ ] MockRenderer.last_rendered_cells: `?[9][9]cell.CellValue` populated from BoardView in render()
- [ ] Test suite exercises real path: GameEngine.fill() → Board.setCell() → render(BoardView) → MockRenderer captures cells — all values match
- [ ] std_renderer.zig, grid.zig, box.zig, renderer.zig, io_sink.zig deleted; root.zig imports 7 modules only
- [ ] Coverage maintained or improved
