Status: ready-for-agent

## Blocked By
- none

## Working mode
HITL. Agent proposes code + tests here, human reviews on paper, then agent executes red→green cycle one test at a time. See `.coding-standards.md` → "TDD methodology (HITL)".

## What to build
AsciiRenderer replacing StdoutRenderer. Drop `RenderCell`, `RenderSnapshot`, and `assembleRenderSnapshot`. Renderer takes an injected writer at construction — renders from BoardView directly, no snapshot copy.

### Why now (after views are in place)

Board has flat storage + lenses (15.1-15.3). The render path still copies 81 cells into an owned `RenderSnapshot` just to iterate them row-by-row — the exact same pattern BoardView replaces for every other read path. AsciiRenderer iterates RowViews through BoardView, writes line-by-line to an injected Writer (stdout in production, memory buffer in tests), and eliminates the snapshot indirection entirely.

### Design notes

- **AsciiRenderer replaces StdoutRenderer.** No OutputSink abstraction — Writer is injected at init time. Tests wire a memory buffer; production wires stdout writer.
- **render() takes BoardView.** Signature becomes `pub fn render(self: *@This(), view: board.BoardView) !void`. Renders directly from the borrowed lens — no snapshot copy.
- **No RenderCell / RenderSnapshot / assembleRenderSnapshot in AsciiRenderer.** Those types stay in place for now (15.5 removes). AsciiRenderer reads Cell values directly from BoardView (`view.board.cells[idx]`).
- **Writer type captured at construction.** AsciiRenderer stores `*std.Io.Writer` — no comptime parameterization. Free-function calls throughout (`std.Io.Writer.writeAll(self.writer, ...)`). The caller must pass the `.interface` field from a buffered writer (e.g. `&outfile.writer(io, &.{ }).interface`) to get the base `*Io.Writer`. Method-style duck-typing (``.writeAll()``) does not work with this API — it requires exactly `*Io.Writer`.

## Existing files touched

| File | What changes |
|------|-------------|
| `src/ascii_renderer.zig` | **New file** — AsciiRenderer (Writer-based), columnHeader/horizBorder pure helpers, cellRow helper. All rendering via BoardView directly.
| `std_renderer.zig` | No changes — StdoutRenderer + OutputSink path stays in place (15.5 removes).
| `root.zig` | Add ascii_renderer import so tests are discovered.
|
### Files NOT touched by 15.4

| File | Reason |
|------|--------|
| `src/cell.zig` | Already provides displayChar/charOf — no change needed.
| `src/io_sink.zig` | IoSink and SinkBuilder are dead code stays in place, removed with box.zig / grid.
| `src/puzzle_gen.zig` | No rendering involvement.
| `src/logger.zig` | Unrelated to renderer refactor.
| `src/box.zig` | Obsolete — removed in 15.5 cleanup, not this slice.
| `src/grid.zig` | Obsolete — removed in 15.5 cleanup, not this slice.
|
### Proposed Code (signatures only)

#### columnHeader()

Copied unchanged from StdoutRenderer. Returns the column label line: `"   A B C | D E F | G H I \n"` with two leading spaces, single-space padding between letters, box dividers at column boundaries, and a trailing newline. Pure helper — no dependencies.


#### horizBorder()

Copied unchanged from StdoutRenderer. Returns the horizontal box-border line: `" +-------+-------+-------+\n"` with plus signs at box boundaries and seven dashes per box. Pure helper — no dependencies.


#### AsciiRenderer — writer-injected renderer from BoardView

**AsciiRenderer** replaces StdoutRenderer. Writer is injected at init time — tests wire a temp file via real `Io.Dir.createFile` → `Io.File.Writer` → `.interface`; production wires stdout writer the same way.

`render()` takes `Board.BoardView` and renders directly from the borrowed lens — no snapshot copy. Iterates rows 0–9, building each data row via internal `cellRow(row, view, buf)` that reads cell values through `view.board.cells[idx]` and formats via `cell.displayChar()`. Border lines inserted after header and after rows 2/5.

Stores `*std.Io.Writer` directly — no comptime type parameterization. The caller resolves the concrete writer at wiring time by passing `&writer.interface` from a buffered file writer.

Existing public seam (`Cell.charOf`, `cell.displayChar`, `Board.asView()`) — all unchanged. AsciiRenderer uses them, adds nothing new to Board.

## Proposed Tests (names + mechanics)
Each test is TDD-style, red→green, co-located in ascii_renderer.zig. All rendering exercises the real code path: Writer writes exact strings that are compared after.

### Test 1 — `"column header"`
Keep existing pure-function test. `columnHeader()` returns exact expected string. Already passes.

### Test 2 — `"horizontal border"`
Same. Pure helper `horizBorder()`, no rendering involved. Already passes.


### Test 3 — `"cell row (empty)"`
Build an empty board (`Board.init()`), create BoardView via `asView()`. Call the internal `cellRow(0, view, &buf)` helper with a stack buffer to render just row index 0. Assert buffer equals `"1|       |       |       |\n"` (row label `1`, all cells empty).

### Test 4 — `"cell row (digits placed)"`

Build a board with some givens via `fromFlat` (row 7 has digits placed). Create BoardView. Call `cellRow()`. Extract the data row for index 7 (line 8 in output, label `8`). Assert buffer equals expected row string with digit values shown.

### Test 5 — `"AsciiRenderer renders empty board end-to-end"`

Use temp file I/O: `Io.Dir.openDirAbsolute(io, "/tmp", .{})`> create a uniquely-named file, open a writer, register AsciiRenderer with `&w.interface`, render the board, flush. Reopen via `Io.Dir.openFile` + `Io.File.reader`, read back the contents with `Io.Reader.readAlloc`, assert full output matches expected ASCII grid (column header + 4 borders + 9 labeled rows all spaces).
### Test 6 — `"AsciiRenderer renders easy puzzle end-to-end"`
Load board via `fromOneLineString(puzzle_gen.PuzzleGen.easy())`. Same temp-file round-trip pattern as Test 5 — render to file, read back with `Io.File.reader` + `readAlloc`, compare full contents against known-good output (the exact ASCII grid already proven correct by current `zig build run`). Identical formatting, only destination differs.

## Acceptance criteria

- [ ] New render tests pass: cell row (empty), cell row (digits placed), end-to-end empty board, end-to-end easy puzzle
- [ ] AsciiRenderer takes injected Writer at init; renders from BoardView directly with zero-copy path
- [ ] Coverage maintained or improved vs 99.16% baseline
- [ ] `zig build` compiles with zero warnings

## Deferred to other sub-issues

| Item | Defer to | Reason |
|------|----------|--------|
| Drop assembleRenderSnapshot from board.zig | 15.5 | Old snapshot path, replaced once AsciiRenderer wired in |
| Wire GameEngine.render() to use BoardView instead of RenderSnapshot | 15.5 | GameEngine contract change |
| Wire MockRenderer to store BoardView instead of RenderSnapshot | 15.5 | Test harness update alongside GameEngine wiring |
| Wire main.zig: inline stdout writer, drop IoSink | 15.5 | Production wiring change |
| Remove renderer.zig (RenderCell/RenderSnapshot) and std_renderer.zig | 15.5 | Dead code removed after AsciiRenderer is wired in |
| Remove box.zig, grid.zig, collapse module structure | 15.5 | Cleanup/rewiring slice — obsolete files don't block rendering work |
| Root zig import audit for final module set | 15.5 | Import hygiene happens alongside file deletion |

---

Triage date: 2026-07-18

Triage notes:
- AsciiRenderer design locked: Writer injected at init (`*std.Io.Writer` base pointer), renders from BoardView directly. Free-function `Io.Writer.writeAll(self.writer, ...)` calls throughout — no comptime type parameterization.
- Scope is renderer-only — no GameEngine, MockRenderer, main.zig, or board.zig changes in this slice.
- Old RenderSnapshot path (StdoutRenderer + assembleRenderSnapshot) stays intact until 15.5 wiring.
- Current state: Tests 1–2 green (pure helpers), Test 3 needs rename to match spec.
